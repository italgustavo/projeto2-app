import { MeusClientesDirective } from './meus-clientes.directive';

describe('MeusClientesDirective', () => {
  it('should create an instance', () => {
    const directive = new MeusClientesDirectiv ();
    expect(directive).toBeTruthy();
  });
});
